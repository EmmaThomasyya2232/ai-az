# Azure AI Manager 进阶重构与全生命周期升级方案

## 一、 升级背景与核心痛点

当前面板已实现 Cloudflare Workers 基础网关、用量日志、节点池以及服务主体占位界面。结合目前的运维实测，系统存在四个关键断点：

1. **凭据无法直接录入**：缺乏解析 Azure CLI 生成的 Service Principal（服务主体）标准 JSON 的 API 与前端弹窗。
2. **缺乏动态区域策略感知**：学生号/受限订阅受到 `RequestDisallowedByAzure` 强管控，盲选区域部署会导致直接报错。
3. **缺乏自动化养号链路**：Free Tier 订阅仅靠纯空置无法触发后台定时提档，缺乏轻量 Embedding 部署与每日微量打卡逻辑。
4. **底层资产不可见**：概览页仅统计网关转发量，无法监控底层订阅的真实 Tier 状态、配额水位及升级限制。

---

## 二、 升级路线图与核心模块

### 模块 1：服务主体智能纳管与订阅自动发现

#### 1. 前端交互

* 在「服务主体」页面的 **「+ 新增服务主体」** 弹窗中增加 **一键粘贴 JSON 文本框**。
* 支持直接粘贴 Azure CLI 生成的标准格式：
```json
{
  "appId": "...",
  "displayName": "...",
  "password": "...",
  "tenant": "..."
}

```


* 前端自动解析并回填字段，同时允许自定义别名（Alias）。

#### 2. 后端接口设计 (`POST /api/service-principals`)

* **OAuth2 验真**：Worker 请求 `[https://login.microsoftonline.com/](https://login.microsoftonline.com/){tenant}/oauth2/v2.0/token` 获取管理令牌（Scope 为 `[https://management.azure.com/.default](https://management.azure.com/.default)`）。
* **订阅穿透提取**：验证通过后自动调用 ARM 接口 `GET /subscriptions?api-version=2022-12-01`，动态拉取该凭据名下绑定的所有有效订阅（提取 `subscriptionId` 与 `displayName`）。
* **安全入库**：凭据写入 Cloudflare D1 数据库，密码（`client_secret`）在落库前进行 AES-GCM 加密存储。

---

### 模块 2：区域白名单动态探测引擎

为杜绝手动部署触发策略拦截（`RequestDisallowedByAzure`），系统需在录入或加载订阅时自动建立区域可用性画像：

1. **策略解析**：
* 调用 ARM Policy 接口：`GET /subscriptions/{subId}/providers/Microsoft.Authorization/policyAssignments?api-version=2022-06-01`。
* 解析 `listOfAllowedLocations` 白名单数组。


2. **探针回退机制**：
* 若策略未显式枚举，通过发送试探性创建请求捕获底层错误文本，自动提取正则匹配的候选区域（如 `["centralus", "canadacentral"]`）。


3. **前端级联限制**：
* 「Azure 资源」页面新增资源组或实例时，区域下拉框**仅展示探测成功的白名单候选区域**，并标记推荐等级。



---

### 模块 3：🌱 养号打卡与提档引擎 (Automated Warmup)

替代手动运行 Cloud Shell 脚本，实现全自动无人值守“种菜”提档。

#### 1. 一键安全上架流水线 (`POST /api/subscriptions/:id/safe-bootstrap`)

在服务主体或资源页提供一键按钮，后台串联执行：

* 匹配最佳合规区域（如 `centralus`）。
* 创建资源组与 S0 规格的 `AIServices` 实例。
* 部署极低风控模型：`text-embedding-3-small`（优先匹配 `GlobalStandard`，失败回退 `Standard`）。
* 发送首次 2-token 调用（`"health check"`）留下非零 Metrics。
* 自动在 D1 中将该实例打上 `is_warming_target = 1` 标签。

#### 2. Cloudflare Cron 定时调度 (每日轻量打卡)

* **执行频次**：设置每日执行 1 次（如 UTC 04:00 / 北京时间 12:00）。
* **打卡行为**：
* 遍历处于 `Free Tier` 且启用了打卡的实例，向已部署的 Embedding 模型发送微量请求。
* 将执行耗时、消耗 Token（通常为 2）、状态码写入 `warmup_logs`。


* **提档感知与推送**：
* 调用 CognitiveServices QuotaTiers API：
`GET [https://management.azure.com/subscriptions/](https://management.azure.com/subscriptions/){subId}/providers/Microsoft.CognitiveServices/quotaTiers/default?api-version=2026-05-01`
* 检测到 `currentTierName` 由 `Free Tier` 变为 `Tier 1` 时：
* 将数据库内订阅状态标记为 `Upgraded`，停止打卡以节省资源。
* 通过 Webhook 向 Telegram Bot 或飞书推送提档成功通知。





---

### 模块 4：📊 Tier 与配额看板 (Radar Dashboard)

在侧边栏新增 **「配额与 Tier」** 模块，直观监控底层资产状态：

* **Tier 状态卡片**：
* 展示当前订阅的 `currentTierName`、分配时间、`tierUpgradePolicy`。
* 详细展示下一档升级资格与不可升级原因（`upgradeUnavailabilityReason`）。


* **打卡流水表格**：
* 展示每日打卡时间、消耗 Tokens、健康状态及响应延迟。


* **配额水位展示**：
* 调用 CognitiveServices Usage API 展现各区域 TPM / RPM 使用率与限额。



---

## 三、 数据库 Schema 演进 (Cloudflare D1)

在现有的 D1 数据库中补充以下表结构与字段：

```sql
-- 1. 扩充服务主体与订阅元数据
ALTER TABLE azure_subscriptions ADD COLUMN subscription_name TEXT;
ALTER TABLE azure_subscriptions ADD COLUMN current_tier TEXT DEFAULT 'Unknown';
ALTER TABLE azure_subscriptions ADD COLUMN allowed_regions TEXT; -- 存储 JSON: ["centralus", "canadacentral"]
ALTER TABLE azure_subscriptions ADD COLUMN warmup_status TEXT DEFAULT 'Pending'; -- Pending / Active / Upgraded / Disabled

-- 2. 新增每日养号打卡流水日志表
CREATE TABLE IF NOT EXISTS warmup_logs (
    id TEXT PRIMARY KEY,
    subscription_id TEXT NOT NULL,
    instance_name TEXT NOT NULL,
    model_name TEXT NOT NULL,
    tokens_consumed INTEGER DEFAULT 0,
    status_code INTEGER,
    response_time_ms INTEGER,
    result_status TEXT, -- Success / Failed
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY(subscription_id) REFERENCES azure_subscriptions(id) ON DELETE CASCADE
);

-- 3. 系统配置表 (通知 Webhook 与全局开关)
CREATE TABLE IF NOT EXISTS system_configs (
    config_key TEXT PRIMARY KEY,
    config_value TEXT NOT NULL
);

```

---

## 四、 实施计划与交付节点

| 阶段 | 交付周期 | 重点工作项 | 验收标准 |
| --- | --- | --- | --- |
| **阶段 1：账号接入闭环** | 1～2 天 | 1. 编写 `POST /api/service-principals`，实现凭据验证与订阅自动发现。<br>

<br>2. 在「服务主体」页面接入 JSON 粘贴弹窗与入库逻辑。 | 粘贴 `appId`/`password`/`tenant` 即可成功识别出关联订阅并入库展示。 |
| **阶段 2：区域白名单与安全编排** | 2 天 | 1. 编写合规区域策略探测逻辑。<br>

<br>2. 封装一键建资源、布 Embedding 模型、首发微调用的后端流程。 | 点击一键初始化，系统自动在合规区域完成资源准备并输出测试结果。 |
| **阶段 3：定时打卡与提档通知** | 2 天 | 1. 在 Worker 的 `scheduled` 事件中接入每日微量打卡与日志入库。<br>

<br>2. 接入 Quota Tier 轮询与 Telegram/飞书 Webhook 提档推送。 | 每日自动打卡无需人工干预，升级后收到推送告警。 |
| **阶段 4：前端看板集成与联调** | 2～3 天 | 1. 在左侧菜单新增「🌱 养号打卡」与「📊 配额与 Tier」视图。<br>

<br>2. 将打卡流水与状态字段可视化呈现，完成整体打包发布。 | 控制台清晰查看所有账号的养号进度与底层配额。 |