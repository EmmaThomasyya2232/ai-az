{
  "name": "azure-ai-manager",
  "main": "src/index.ts",
  "compatibility_date": "2025-01-01",
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "run_worker_first": true
  },
  // 阶段二: D1 持久层。database_id 以下为已创建的远程数据库:
  // - CLI 一键部署: npm run setup (复用/创建 D1 并回填 id, 幂等可重复执行)
  // - 线上一键部署: Deploy to Cloudflare 按钮直接跑 npx wrangler deploy, 要求本文件含真实 id
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "azure-ai-manager",
      "database_id": "bee51106-d860-4707-a15a-326690cb65c7",
      "migrations_dir": "migrations"
    }
  ],
  // 阶段五/六: 定时巡检 (节点探活 / 令牌预热 / 日志清理) 与每日养号打卡 (UTC 04:00 ≈ 北京时间 12:00),
  // 任务内容按 cron 匹配: */5 只跑巡检, 每日 04:00 额外跑一次养号打卡
  // 本地测试: curl "http://localhost:8787/__scheduled?cron=*/5+*+*+*+*"  /  "0+4+*+*+*"
  "triggers": {
    "crons": ["*/5 * * * *", "0 4 * * *"]
  }
}
